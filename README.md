# Select component

Веб-приложение для выбора опций с расширенным компонентом Select, включающим поиск, клавиатурную навигацию и автоматическое позиционирование выпадающего списка.

## Содержание

- [Описание](#описание)
- [Технологии](#технологии)
- [Установка и запуск](#установка-и-запуск)
- [API документация](#api-документация)

## Описание

Приложение позволяет пользователям:
- Просматривать список из 1000 опций (номера от 1 до 1000)
- Искать опции по названию с фильтрацией в реальном времени
- Выбирать опцию с помощью мыши или клавиатуры
- Отправлять выбранную опцию на сервер
- Получать подтверждение об успешной отправке

### Основные возможности

-  **Поиск в реальном времени** - фильтрация опций при вводе текста
-  **Клавиатурная навигация** - полная поддержка навигации с клавиатуры
-  **Автопозиционирование** - выпадающий список автоматически открывается вверх или вниз в зависимости от доступного пространства
-  **Доступность** - поддержка ARIA атрибутов для screen readers
-  **Производительность** - оптимизированная работа с большими списками

## Технологии

### Frontend
- **React 19.2.0** - библиотека для создания пользовательского интерфейса
- **TypeScript 5.9.3** - типизированный JavaScript
- **Redux Toolkit 2.10.1** - управление состоянием приложения
- **Vite 7.2.2** - сборщик и dev-сервер
- **CSS Modules** - модульные стили

### Backend
- **Node.js** - серверная платформа
- **Express 4.21.2** - веб-фреймворк
- **TypeScript 5.9.3** - типизированный JavaScript
- **LowDB 7.0.1** - легковесная JSON база данных
- **CORS 2.8.5** - поддержка кросс-доменных запросов

### Инструменты разработки
- **ESLint** - линтер для JavaScript/TypeScript
- **ts-node-dev** - разработка с hot reload
- **Concurrently** - параллельный запуск frontend и backend

## Установка и запуск

### Требования

- Node.js (версия 18 или выше)
- npm или yarn

### Установка зависимостей

```bash
# Установка зависимостей для всего проекта
npm install

# Или установка по отдельности
cd backend && npm install
cd ../frontend && npm install
```

### Настройка переменных окружения

Создайте файл `.env` в папке `frontend/`:

```env
VITE_API_URL=http://localhost:8000
```

### Запуск в режиме разработки

```bash
# Запуск frontend и backend одновременно
npm run dev

# Или запуск по отдельности
npm run dev:backend    # Backend на http://localhost:8000
npm run dev:frontend   # Frontend на http://localhost:5173
```

### Сборка для production

```bash
# Сборка всего проекта
npm run build

# Или сборка по отдельности
npm run build:backend
npm run build:frontend
```

### Запуск production версии

```bash
npm start
```

## API документация

### Endpoints

#### GET `/options/for/select`

Получение списка всех доступных опций.

**Ответ:**
```json
[
  {
    "name": "1",
    "value": "1"
  },
  {
    "name": "2",
    "value": "2"
  },
  ...
]
```

**Статус коды:**
- `200` - Успешный запрос
- `500` - Ошибка сервера

---

#### POST `/selected/option`

Отправка выбранной опции на сервер.

**Тело запроса:**
```json
{
  "value": "123"
}
```

**Валидация:**
- `value` обязателен
- `value` должен быть числом от 1 до 1000

**Ответ:**
```json
{
  "message": "Выбранная опция 123 успешно принята."
}
```

**Статус коды:**
- `200` - Успешная отправка
- `400` - Неверный формат данных или значение вне диапазона
- `500` - Ошибка сервера

**Пример ошибки:**
```json
{
  "success": false,
  "error": "Value must be a number between 1 and 1000",
  "status": 400
}
```

### Линтинг

```bash
# Проверка кода
cd frontend && npm run lint
```

## 📝 Примечания

- Backend использует порт `8000` по умолчанию
- Frontend использует порт `5173` (Vite default)
- CORS настроен для `http://localhost:5173`
- База данных автоматически инициализируется при первом запуске
- Все опции создаются автоматически (1-1000)

